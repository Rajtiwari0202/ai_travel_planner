# Data Privacy

The public demo uses anonymous session-scoped access. The raw browser token is not stored; only a SHA-256 hash is stored.

Do not enter sensitive travel documents, payment details, or real booking credentials.

Demo data expires according to `ANONYMOUS_TRIP_TTL_DAYS`.
